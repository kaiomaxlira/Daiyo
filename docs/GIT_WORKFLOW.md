# Git Workflow

main = stable

dev = development

feature/* = individual features

Feature branches must be created from dev.

Pull Requests merge feature/* into dev.

Stable releases are merged from dev into main.
